#pragma once

#include "${CUDA_MULTIPLIER}.hpp"

// clang-format off
struct ${HOST_CUDA_MULTIPLIER} {
  ${CUDA_MULTIPLIER} multiplier{};

  void operator()(std::size_t n, double const* a, double const* b, double* c);
};
// clang-format on
