#pragma once

// clang-format off
#include "${PAR_EXEC_HEADER_FILE}"

struct ${MULTIPLIER} {
#define ${PAR_EXEC_BASENAME}
#ifdef ThreadPoolExecutorTemplate
#undef ${PAR_EXEC_BASENAME}
  ${PARALLEL_EXECUTOR} exe{};
#endif
#undef ${PAR_EXEC_BASENAME}
  void operator()(std::size_t n, double const* a, double const* b, double* c);
};
// clang-format on
