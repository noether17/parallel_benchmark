#pragma once

// clang-format off
#include "${PAR_EXEC_BASENAME}.${HEADER_FILE_EXT}"

using namespace ParODE;

struct ${PAR_EXEC_TOKEN}_multiply {
  ${PAR_EXEC_TYPE} exe{};
  void operator()(std::size_t n, double const* a, double const* b, double* c);
};
// clang-format on
