#pragma once

#include <cstddef>

// clang-format off
#include "${PARD_HEADER_FILE}"

struct ${MULTIPLIER} {
  ParD::${PARD_TYPE} exe{};
  void operator()(std::size_t n, double const* a, double const* b, double* c);
};
// clang-format on
