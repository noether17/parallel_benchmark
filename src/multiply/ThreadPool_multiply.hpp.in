#pragma once

#include <cstddef>

// clang-format off
#include "${PARX_HEADER_FILE}"

struct ${MULTIPLIER} {
  ${PARX_TYPE} exe{};
  void operator()(std::size_t n, double const* a, double const* b, double* c);
  void synchronize() const { ParX::synchronize(exe); }
};
// clang-format on
