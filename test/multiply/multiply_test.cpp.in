#include <gtest/gtest.h>

#include <random>
#include <vector>

// clang-format off
#include "${MULTIPLIER_HEADER}"
#define ${MULTIPLIER_TYPE}
#ifdef CUDA_MULTIPLIER
#include "multiply/cuda_multiply.cuh"
#endif

static constexpr auto N = 1 << 10;

TEST(${TEST_NAME}, ProducesCorrectOutput) {
  auto multiply = ${MULTIPLIER}{};
  auto a = std::vector<double>(N);
  auto b = std::vector<double>(N);
  auto c = std::vector<double>(N);
  auto gen = std::mt19937{};
  auto dist = std::uniform_real_distribution<double>{0.0, 1.0};
  for (auto& x : a) {
    x = dist(gen);
  }
  for (auto& y : b) {
    y = dist(gen);
  }

#ifdef CUDA_MULTIPLIER
  cuda_multiply_host_data(multiply, N, a.data(), b.data(), c.data());
#else
  multiply(N, a.data(), b.data(), c.data());
#endif

  for (auto i = 0; i < N; ++i) {
    EXPECT_DOUBLE_EQ(a[i] * b[i], c[i]);
  }
}
// clang-format on
